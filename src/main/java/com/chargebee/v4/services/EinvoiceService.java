/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.services;

import com.chargebee.v4.client.ChargebeeClient;
import com.chargebee.v4.client.request.RequestOptions;
import com.chargebee.v4.exceptions.ChargebeeException;
import com.chargebee.v4.transport.Response;
import java.util.concurrent.CompletableFuture;

import com.chargebee.v4.models.einvoice.params.ListEinvoicesParams;

import com.chargebee.v4.models.einvoice.responses.ListEinvoicesResponse;

import com.chargebee.v4.models.einvoice.responses.EinvoiceRetrieveResponse;

public final class EinvoiceService extends BaseService<EinvoiceService> {

  private final ServiceConfig config;

  public EinvoiceService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private EinvoiceService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private EinvoiceService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  EinvoiceService with(RequestOptions newOptions) {
    return new EinvoiceService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public EinvoiceService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /**
   * listEinvoices a einvoice using immutable params (executes immediately) - returns raw Response.
   */
  Response listEinvoicesRaw(ListEinvoicesParams params) throws ChargebeeException {

    return get(
        "einvoice", "listEinvoices", "/einvoices", params != null ? params.toQueryParams() : null);
  }

  /** listEinvoices a einvoice without params (executes immediately) - returns raw Response. */
  Response listEinvoicesRaw() throws ChargebeeException {

    return get("einvoice", "listEinvoices", "/einvoices", null);
  }

  /**
   * listEinvoices a einvoice using raw JSON payload (executes immediately) - returns raw Response.
   */
  Response listEinvoicesRaw(String jsonPayload) throws ChargebeeException {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public ListEinvoicesResponse listEinvoices(ListEinvoicesParams params) throws ChargebeeException {
    Response response = listEinvoicesRaw(params);

    return ListEinvoicesResponse.fromJson(response.getBodyAsString(), this, params, response);
  }

  /** Async variant of listEinvoices for einvoice with params. */
  public CompletableFuture<ListEinvoicesResponse> listEinvoicesAsync(ListEinvoicesParams params) {

    return getAsync(
            "einvoice",
            "listEinvoices",
            "/einvoices",
            params != null ? params.toQueryParams() : null)
        .thenApply(
            response ->
                ListEinvoicesResponse.fromJson(response.getBodyAsString(), this, params, response));
  }

  public ListEinvoicesResponse listEinvoices() throws ChargebeeException {
    Response response = listEinvoicesRaw();

    return ListEinvoicesResponse.fromJson(response.getBodyAsString(), this, null, response);
  }

  /** Async variant of listEinvoices for einvoice without params. */
  public CompletableFuture<ListEinvoicesResponse> listEinvoicesAsync() {

    return getAsync("einvoice", "listEinvoices", "/einvoices", null)
        .thenApply(
            response ->
                ListEinvoicesResponse.fromJson(response.getBodyAsString(), this, null, response));
  }

  /** retrieve a einvoice (executes immediately) - returns raw Response. */
  Response retrieveRaw(String einvoiceId) throws ChargebeeException {
    String path = buildPathWithParams("/einvoices/{einvoice-id}", "einvoice-id", einvoiceId);

    return get("einvoice", "retrieve", path, null);
  }

  public EinvoiceRetrieveResponse retrieve(String einvoiceId) throws ChargebeeException {
    Response response = retrieveRaw(einvoiceId);
    return EinvoiceRetrieveResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of retrieve for einvoice without params. */
  public CompletableFuture<EinvoiceRetrieveResponse> retrieveAsync(String einvoiceId) {
    String path = buildPathWithParams("/einvoices/{einvoice-id}", "einvoice-id", einvoiceId);

    return getAsync("einvoice", "retrieve", path, null)
        .thenApply(
            response -> EinvoiceRetrieveResponse.fromJson(response.getBodyAsString(), response));
  }
}
