/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.services;

import com.chargebee.v4.client.ChargebeeClient;
import com.chargebee.v4.client.request.RequestOptions;
import com.chargebee.v4.exceptions.ChargebeeException;
import com.chargebee.v4.transport.Response;
import java.util.concurrent.CompletableFuture;

import com.chargebee.v4.models.meter.params.MeterListParams;

import com.chargebee.v4.models.meter.responses.MeterListResponse;

public final class MeterService extends BaseService<MeterService> {

  private final ServiceConfig config;

  public MeterService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private MeterService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private MeterService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  MeterService with(RequestOptions newOptions) {
    return new MeterService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public MeterService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** list a meter using immutable params (executes immediately) - returns raw Response. */
  Response listRaw(MeterListParams params) throws ChargebeeException {

    return get("meter", "list", "/meters", params != null ? params.toQueryParams() : null);
  }

  /** list a meter without params (executes immediately) - returns raw Response. */
  Response listRaw() throws ChargebeeException {

    return get("meter", "list", "/meters", null);
  }

  /** list a meter using raw JSON payload (executes immediately) - returns raw Response. */
  Response listRaw(String jsonPayload) throws ChargebeeException {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public MeterListResponse list(MeterListParams params) throws ChargebeeException {
    Response response = listRaw(params);

    return MeterListResponse.fromJson(response.getBodyAsString(), this, params, response);
  }

  /** Async variant of list for meter with params. */
  public CompletableFuture<MeterListResponse> listAsync(MeterListParams params) {

    return getAsync("meter", "list", "/meters", params != null ? params.toQueryParams() : null)
        .thenApply(
            response ->
                MeterListResponse.fromJson(response.getBodyAsString(), this, params, response));
  }

  public MeterListResponse list() throws ChargebeeException {
    Response response = listRaw();

    return MeterListResponse.fromJson(response.getBodyAsString(), this, null, response);
  }

  /** Async variant of list for meter without params. */
  public CompletableFuture<MeterListResponse> listAsync() {

    return getAsync("meter", "list", "/meters", null)
        .thenApply(
            response ->
                MeterListResponse.fromJson(response.getBodyAsString(), this, null, response));
  }
}
