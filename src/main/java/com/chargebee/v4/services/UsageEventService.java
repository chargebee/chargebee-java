/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.services;

import com.chargebee.v4.client.ChargebeeClient;
import com.chargebee.v4.client.request.RequestOptions;
import com.chargebee.v4.exceptions.ChargebeeException;
import com.chargebee.v4.transport.Response;
import java.util.concurrent.CompletableFuture;

import com.chargebee.v4.models.usageEvent.params.UsageEventCreateParams;

import com.chargebee.v4.models.usageEvent.params.UsageEventBatchIngestParams;

import com.chargebee.v4.models.usageEvent.responses.UsageEventCreateResponse;

import com.chargebee.v4.models.usageEvent.responses.UsageEventBatchIngestResponse;

import com.chargebee.v4.internal.SubDomain;

public final class UsageEventService extends BaseService<UsageEventService> {

  private final ServiceConfig config;

  public UsageEventService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private UsageEventService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private UsageEventService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  UsageEventService with(RequestOptions newOptions) {
    return new UsageEventService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public UsageEventService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** create a usageEvent using immutable params (executes immediately) - returns raw Response. */
  Response createRaw(UsageEventCreateParams params) throws ChargebeeException {

    return postJsonWithSubDomain(
        "usageEvent",
        "create",
        "/usage_events",
        SubDomain.INGEST.getValue(),
        params != null ? params.toJsonString() : null);
  }

  /** create a usageEvent using raw JSON payload (executes immediately) - returns raw Response. */
  Response createRaw(String jsonPayload) throws ChargebeeException {

    return postJsonWithSubDomain(
        "usageEvent", "create", "/usage_events", SubDomain.INGEST.getValue(), jsonPayload);
  }

  public UsageEventCreateResponse create(UsageEventCreateParams params) throws ChargebeeException {
    Response response = createRaw(params);

    return UsageEventCreateResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of create for usageEvent with params. */
  public CompletableFuture<UsageEventCreateResponse> createAsync(UsageEventCreateParams params) {

    return postJsonWithSubDomainAsync(
            "usageEvent",
            "create",
            "/usage_events",
            SubDomain.INGEST.getValue(),
            params != null ? params.toJsonString() : null)
        .thenApply(
            response -> UsageEventCreateResponse.fromJson(response.getBodyAsString(), response));
  }

  /**
   * batchIngest a usageEvent using immutable params (executes immediately) - returns raw Response.
   */
  Response batchIngestRaw(UsageEventBatchIngestParams params) throws ChargebeeException {

    return postJsonWithSubDomain(
        "usageEvent",
        "batchIngest",
        "/batch/usage_events",
        SubDomain.INGEST.getValue(),
        params != null ? params.toJsonString() : null);
  }

  /**
   * batchIngest a usageEvent using raw JSON payload (executes immediately) - returns raw Response.
   */
  Response batchIngestRaw(String jsonPayload) throws ChargebeeException {

    return postJsonWithSubDomain(
        "usageEvent",
        "batchIngest",
        "/batch/usage_events",
        SubDomain.INGEST.getValue(),
        jsonPayload);
  }

  public UsageEventBatchIngestResponse batchIngest(UsageEventBatchIngestParams params)
      throws ChargebeeException {
    Response response = batchIngestRaw(params);

    return UsageEventBatchIngestResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of batchIngest for usageEvent with params. */
  public CompletableFuture<UsageEventBatchIngestResponse> batchIngestAsync(
      UsageEventBatchIngestParams params) {

    return postJsonWithSubDomainAsync(
            "usageEvent",
            "batchIngest",
            "/batch/usage_events",
            SubDomain.INGEST.getValue(),
            params != null ? params.toJsonString() : null)
        .thenApply(
            response ->
                UsageEventBatchIngestResponse.fromJson(response.getBodyAsString(), response));
  }
}
