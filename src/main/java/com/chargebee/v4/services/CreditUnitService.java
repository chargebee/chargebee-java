/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.services;

import com.chargebee.v4.client.ChargebeeClient;
import com.chargebee.v4.client.request.RequestOptions;
import com.chargebee.v4.exceptions.ChargebeeException;
import com.chargebee.v4.transport.Response;
import java.util.concurrent.CompletableFuture;

import com.chargebee.v4.models.creditUnit.params.CreditUnitListParams;

import com.chargebee.v4.models.creditUnit.params.CreditUnitCreateParams;

import com.chargebee.v4.models.creditUnit.params.CreditUnitUpdateParams;

import com.chargebee.v4.models.creditUnit.responses.CreditUnitListResponse;

import com.chargebee.v4.models.creditUnit.responses.CreditUnitCreateResponse;

import com.chargebee.v4.models.creditUnit.responses.CreditUnitArchiveResponse;

import com.chargebee.v4.models.creditUnit.responses.CreditUnitUpdateResponse;

import com.chargebee.v4.models.creditUnit.responses.CreditUnitReactivateResponse;

public final class CreditUnitService extends BaseService<CreditUnitService> {

  private final ServiceConfig config;

  public CreditUnitService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private CreditUnitService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private CreditUnitService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  CreditUnitService with(RequestOptions newOptions) {
    return new CreditUnitService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public CreditUnitService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** list a creditUnit using immutable params (executes immediately) - returns raw Response. */
  Response listRaw(CreditUnitListParams params) throws ChargebeeException {

    return get(
        "creditUnit", "list", "/credit_units", params != null ? params.toQueryParams() : null);
  }

  /** list a creditUnit without params (executes immediately) - returns raw Response. */
  Response listRaw() throws ChargebeeException {

    return get("creditUnit", "list", "/credit_units", null);
  }

  /** list a creditUnit using raw JSON payload (executes immediately) - returns raw Response. */
  Response listRaw(String jsonPayload) throws ChargebeeException {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public CreditUnitListResponse list(CreditUnitListParams params) throws ChargebeeException {
    Response response = listRaw(params);

    return CreditUnitListResponse.fromJson(response.getBodyAsString(), this, params, response);
  }

  /** Async variant of list for creditUnit with params. */
  public CompletableFuture<CreditUnitListResponse> listAsync(CreditUnitListParams params) {

    return getAsync(
            "creditUnit", "list", "/credit_units", params != null ? params.toQueryParams() : null)
        .thenApply(
            response ->
                CreditUnitListResponse.fromJson(
                    response.getBodyAsString(), this, params, response));
  }

  public CreditUnitListResponse list() throws ChargebeeException {
    Response response = listRaw();

    return CreditUnitListResponse.fromJson(response.getBodyAsString(), this, null, response);
  }

  /** Async variant of list for creditUnit without params. */
  public CompletableFuture<CreditUnitListResponse> listAsync() {

    return getAsync("creditUnit", "list", "/credit_units", null)
        .thenApply(
            response ->
                CreditUnitListResponse.fromJson(response.getBodyAsString(), this, null, response));
  }

  /** create a creditUnit using immutable params (executes immediately) - returns raw Response. */
  Response createRaw(CreditUnitCreateParams params) throws ChargebeeException {

    return post(
        "creditUnit", "create", "/credit_units", params != null ? params.toFormData() : null);
  }

  /** create a creditUnit using raw JSON payload (executes immediately) - returns raw Response. */
  Response createRaw(String jsonPayload) throws ChargebeeException {

    return postJson("creditUnit", "create", "/credit_units", jsonPayload);
  }

  public CreditUnitCreateResponse create(CreditUnitCreateParams params) throws ChargebeeException {
    Response response = createRaw(params);

    return CreditUnitCreateResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of create for creditUnit with params. */
  public CompletableFuture<CreditUnitCreateResponse> createAsync(CreditUnitCreateParams params) {

    return postAsync(
            "creditUnit", "create", "/credit_units", params != null ? params.toFormData() : null)
        .thenApply(
            response -> CreditUnitCreateResponse.fromJson(response.getBodyAsString(), response));
  }

  /** archive a creditUnit (executes immediately) - returns raw Response. */
  Response archiveRaw(String creditUnitId) throws ChargebeeException {
    String path =
        buildPathWithParams(
            "/credit_units/{credit-unit-id}/archive_command", "credit-unit-id", creditUnitId);

    return post("creditUnit", "archive", path, null);
  }

  public CreditUnitArchiveResponse archive(String creditUnitId) throws ChargebeeException {
    Response response = archiveRaw(creditUnitId);
    return CreditUnitArchiveResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of archive for creditUnit without params. */
  public CompletableFuture<CreditUnitArchiveResponse> archiveAsync(String creditUnitId) {
    String path =
        buildPathWithParams(
            "/credit_units/{credit-unit-id}/archive_command", "credit-unit-id", creditUnitId);

    return postAsync("creditUnit", "archive", path, null)
        .thenApply(
            response -> CreditUnitArchiveResponse.fromJson(response.getBodyAsString(), response));
  }

  /** update a creditUnit (executes immediately) - returns raw Response. */
  Response updateRaw(String creditUnitId) throws ChargebeeException {
    String path =
        buildPathWithParams("/credit_units/{credit-unit-id}", "credit-unit-id", creditUnitId);

    return post("creditUnit", "update", path, null);
  }

  /** update a creditUnit using immutable params (executes immediately) - returns raw Response. */
  Response updateRaw(String creditUnitId, CreditUnitUpdateParams params) throws ChargebeeException {
    String path =
        buildPathWithParams("/credit_units/{credit-unit-id}", "credit-unit-id", creditUnitId);
    return post("creditUnit", "update", path, params.toFormData());
  }

  /** update a creditUnit using raw JSON payload (executes immediately) - returns raw Response. */
  Response updateRaw(String creditUnitId, String jsonPayload) throws ChargebeeException {
    String path =
        buildPathWithParams("/credit_units/{credit-unit-id}", "credit-unit-id", creditUnitId);
    return postJson("creditUnit", "update", path, jsonPayload);
  }

  public CreditUnitUpdateResponse update(String creditUnitId, CreditUnitUpdateParams params)
      throws ChargebeeException {
    Response response = updateRaw(creditUnitId, params);
    return CreditUnitUpdateResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of update for creditUnit with params. */
  public CompletableFuture<CreditUnitUpdateResponse> updateAsync(
      String creditUnitId, CreditUnitUpdateParams params) {
    String path =
        buildPathWithParams("/credit_units/{credit-unit-id}", "credit-unit-id", creditUnitId);
    return postAsync("creditUnit", "update", path, params.toFormData())
        .thenApply(
            response -> CreditUnitUpdateResponse.fromJson(response.getBodyAsString(), response));
  }

  public CreditUnitUpdateResponse update(String creditUnitId) throws ChargebeeException {
    Response response = updateRaw(creditUnitId);
    return CreditUnitUpdateResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of update for creditUnit without params. */
  public CompletableFuture<CreditUnitUpdateResponse> updateAsync(String creditUnitId) {
    String path =
        buildPathWithParams("/credit_units/{credit-unit-id}", "credit-unit-id", creditUnitId);

    return postAsync("creditUnit", "update", path, null)
        .thenApply(
            response -> CreditUnitUpdateResponse.fromJson(response.getBodyAsString(), response));
  }

  /** reactivate a creditUnit (executes immediately) - returns raw Response. */
  Response reactivateRaw(String creditUnitId) throws ChargebeeException {
    String path =
        buildPathWithParams(
            "/credit_units/{credit-unit-id}/reactivate_command", "credit-unit-id", creditUnitId);

    return post("creditUnit", "reactivate", path, null);
  }

  public CreditUnitReactivateResponse reactivate(String creditUnitId) throws ChargebeeException {
    Response response = reactivateRaw(creditUnitId);
    return CreditUnitReactivateResponse.fromJson(response.getBodyAsString(), response);
  }

  /** Async variant of reactivate for creditUnit without params. */
  public CompletableFuture<CreditUnitReactivateResponse> reactivateAsync(String creditUnitId) {
    String path =
        buildPathWithParams(
            "/credit_units/{credit-unit-id}/reactivate_command", "credit-unit-id", creditUnitId);

    return postAsync("creditUnit", "reactivate", path, null)
        .thenApply(
            response ->
                CreditUnitReactivateResponse.fromJson(response.getBodyAsString(), response));
  }
}
