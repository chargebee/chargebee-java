/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.core.services;

import com.chargebee.v4.client.ChargebeeClient;
import com.chargebee.v4.client.request.RequestOptions;
import com.chargebee.v4.transport.Response;

import com.chargebee.v4.core.models.fullExport.params.FullExportStatusParams;

import com.chargebee.v4.core.responses.fullExport.FullExportStatusResponse;

public final class FullExportService extends BaseService<FullExportService> {

  private final ServiceConfig config;

  public FullExportService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private FullExportService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private FullExportService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  FullExportService with(RequestOptions newOptions) {
    return new FullExportService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public FullExportService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** status a fullExport using immutable params (executes immediately) - returns raw Response. */
  Response statusRaw(FullExportStatusParams params) throws Exception {

    return get("/full_exports/status", params != null ? params.toQueryParams() : null);
  }

  /** status a fullExport using raw JSON payload (executes immediately) - returns raw Response. */
  Response statusRaw(String jsonPayload) throws Exception {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public FullExportStatusResponse status(FullExportStatusParams params) throws Exception {
    Response response = statusRaw(params);

    return parseResponse(response, FullExportStatusResponse.class);
  }
}
