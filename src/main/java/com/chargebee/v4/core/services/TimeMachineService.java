/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.core.services;

import com.chargebee.v4.client.ChargebeeClient;
import com.chargebee.v4.client.request.RequestOptions;
import com.chargebee.v4.transport.Response;

import com.chargebee.v4.core.models.timeMachine.params.TimeMachineTravelForwardParams;

import com.chargebee.v4.core.models.timeMachine.params.TimeMachineStartAfreshParams;

import com.chargebee.v4.core.responses.timeMachine.TimeMachineRetrieveResponse;

import com.chargebee.v4.core.responses.timeMachine.TimeMachineTravelForwardResponse;

import com.chargebee.v4.core.responses.timeMachine.TimeMachineStartAfreshResponse;

public final class TimeMachineService extends BaseService<TimeMachineService> {

  private final ServiceConfig config;

  public TimeMachineService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private TimeMachineService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private TimeMachineService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  TimeMachineService with(RequestOptions newOptions) {
    return new TimeMachineService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public TimeMachineService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** retrieve a timeMachine (executes immediately) - returns raw Response. */
  Response retrieveRaw(String timeMachineName) throws Exception {
    String path =
        buildPathWithParams(
            "/time_machines/{time-machine-name}", "time-machine-name", timeMachineName);

    return get(path, null);
  }

  public TimeMachineRetrieveResponse retrieve(String timeMachineName) throws Exception {
    Response response = retrieveRaw(timeMachineName);
    return parseResponse(response, TimeMachineRetrieveResponse.class);
  }

  /** travelForward a timeMachine (executes immediately) - returns raw Response. */
  Response travelForwardRaw(String timeMachineName) throws Exception {
    String path =
        buildPathWithParams(
            "/time_machines/{time-machine-name}/travel_forward",
            "time-machine-name",
            timeMachineName);

    return post(path, null);
  }

  /**
   * travelForward a timeMachine using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response travelForwardRaw(String timeMachineName, TimeMachineTravelForwardParams params)
      throws Exception {
    String path =
        buildPathWithParams(
            "/time_machines/{time-machine-name}/travel_forward",
            "time-machine-name",
            timeMachineName);
    return post(path, params.toFormData());
  }

  /**
   * travelForward a timeMachine using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response travelForwardRaw(String timeMachineName, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/time_machines/{time-machine-name}/travel_forward",
            "time-machine-name",
            timeMachineName);
    return postJson(path, jsonPayload);
  }

  public TimeMachineTravelForwardResponse travelForward(
      String timeMachineName, TimeMachineTravelForwardParams params) throws Exception {
    Response response = travelForwardRaw(timeMachineName, params);
    return parseResponse(response, TimeMachineTravelForwardResponse.class);
  }

  /** startAfresh a timeMachine (executes immediately) - returns raw Response. */
  Response startAfreshRaw(String timeMachineName) throws Exception {
    String path =
        buildPathWithParams(
            "/time_machines/{time-machine-name}/start_afresh",
            "time-machine-name",
            timeMachineName);

    return post(path, null);
  }

  /**
   * startAfresh a timeMachine using immutable params (executes immediately) - returns raw Response.
   */
  Response startAfreshRaw(String timeMachineName, TimeMachineStartAfreshParams params)
      throws Exception {
    String path =
        buildPathWithParams(
            "/time_machines/{time-machine-name}/start_afresh",
            "time-machine-name",
            timeMachineName);
    return post(path, params.toFormData());
  }

  /**
   * startAfresh a timeMachine using raw JSON payload (executes immediately) - returns raw Response.
   */
  Response startAfreshRaw(String timeMachineName, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/time_machines/{time-machine-name}/start_afresh",
            "time-machine-name",
            timeMachineName);
    return postJson(path, jsonPayload);
  }

  public TimeMachineStartAfreshResponse startAfresh(
      String timeMachineName, TimeMachineStartAfreshParams params) throws Exception {
    Response response = startAfreshRaw(timeMachineName, params);
    return parseResponse(response, TimeMachineStartAfreshResponse.class);
  }
}
