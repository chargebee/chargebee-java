/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */
package com.chargebee.v4.core.models.invoice.params;

import com.chargebee.v4.internal.Recommended;

import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.List;

public final class InvoiceDeleteLineItemsParams {

  private final Map<String, Object> formData;

  private InvoiceDeleteLineItemsParams(InvoiceDeleteLineItemsBuilder builder) {
    this.formData = Collections.unmodifiableMap(new LinkedHashMap<>(builder.formData));
  }

  /** Get the form data for this request. */
  public Map<String, Object> toFormData() {
    return formData;
  }

  /** Create a new builder for InvoiceDeleteLineItemsParams. */
  @Recommended(reason = "Preferred for reusability, validation, and LLM-friendliness")
  public static InvoiceDeleteLineItemsBuilder builder() {
    return new InvoiceDeleteLineItemsBuilder();
  }

  public static final class InvoiceDeleteLineItemsBuilder {
    private final Map<String, Object> formData = new LinkedHashMap<>();

    private InvoiceDeleteLineItemsBuilder() {}

    public InvoiceDeleteLineItemsBuilder lineItems(List<LineItemsParams> value) {
      // Convert list of individual items into indexed format
      if (value != null && !value.isEmpty()) {
        for (int i = 0; i < value.size(); i++) {
          LineItemsParams item = value.get(i);
          if (item != null) {
            Map<String, Object> itemData = item.toFormData();
            for (Map.Entry<String, Object> entry : itemData.entrySet()) {
              String indexedKey = "line_items[" + entry.getKey() + "][" + i + "]";
              formData.put(indexedKey, entry.getValue());
            }
          }
        }
      }
      return this;
    }

    public InvoiceDeleteLineItemsParams build() {
      return new InvoiceDeleteLineItemsParams(this);
    }
  }

  public static final class LineItemsParams {

    private final Map<String, Object> formData;

    private LineItemsParams(LineItemsBuilder builder) {
      this.formData = Collections.unmodifiableMap(new LinkedHashMap<>(builder.formData));
    }

    /** Get the form data for this request. */
    public Map<String, Object> toFormData() {
      return formData;
    }

    /** Create a new builder for LineItemsParams. */
    @Recommended(reason = "Preferred for reusability, validation, and LLM-friendliness")
    public static LineItemsBuilder builder() {
      return new LineItemsBuilder();
    }

    public static final class LineItemsBuilder {
      private final Map<String, Object> formData = new LinkedHashMap<>();

      private LineItemsBuilder() {}

      public LineItemsBuilder id(String value) {
        formData.put("id", value);
        return this;
      }

      public LineItemsParams build() {
        return new LineItemsParams(this);
      }
    }
  }
}
