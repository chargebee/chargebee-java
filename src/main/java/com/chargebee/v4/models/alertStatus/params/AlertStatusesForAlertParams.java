/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.models.alertStatus.params;

import com.chargebee.v4.internal.Recommended;
import com.chargebee.v4.filters.EnumFilter;

import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.Map;

public final class AlertStatusesForAlertParams {

  private final Map<String, Object> queryParams;

  private AlertStatusesForAlertParams(AlertStatusesForAlertBuilder builder) {
    this.queryParams = Collections.unmodifiableMap(new LinkedHashMap<>(builder.queryParams));
  }

  /** Get the query parameters for this request. */
  public Map<String, Object> toQueryParams() {
    return queryParams;
  }

  public AlertStatusesForAlertBuilder toBuilder() {
    AlertStatusesForAlertBuilder builder = new AlertStatusesForAlertBuilder();
    builder.queryParams.putAll(queryParams);
    return builder;
  }

  /** Create a new builder for AlertStatusesForAlertParams. */
  @Recommended(reason = "Preferred for reusability, validation, and LLM-friendliness")
  public static AlertStatusesForAlertBuilder builder() {
    return new AlertStatusesForAlertBuilder();
  }

  public static final class AlertStatusesForAlertBuilder {
    private final Map<String, Object> queryParams = new LinkedHashMap<>();

    private AlertStatusesForAlertBuilder() {}

    public AlertStatusesForAlertBuilder limit(Integer value) {
      queryParams.put("limit", value);
      return this;
    }

    public AlertStatusesForAlertBuilder offset(String value) {
      queryParams.put("offset", value);
      return this;
    }

    public AlarmStatusFilter alarmStatus() {
      return new AlarmStatusFilter("alarm_status", this, queryParams);
    }

    public AlertStatusesForAlertParams build() {
      return new AlertStatusesForAlertParams(this);
    }

    public static final class AlarmStatusFilter
        extends EnumFilter<AlarmStatus, AlertStatusesForAlertBuilder> {
      AlarmStatusFilter(
          String fieldName, AlertStatusesForAlertBuilder builder, Map<String, Object> params) {
        super(fieldName, builder, params, AlarmStatus::getValue);
      }

      /**
       * @deprecated This method accepting raw String will be removed in a future version. Use the
       *     type-safe enum overload instead:
       *     <pre>{@code .alarmStatus().is(AlarmStatus.YOUR_VALUE)}</pre>
       *
       * @see #is(AlarmStatus)
       */
      @Deprecated
      public AlertStatusesForAlertBuilder is(String value) {
        params.put(fieldName + "[is]", value);
        return builder;
      }

      /**
       * @deprecated This method accepting raw String will be removed in a future version. Use the
       *     type-safe enum overload instead:
       *     <pre>{@code .alarmStatus().isNot(AlarmStatus.YOUR_VALUE)}</pre>
       *
       * @see #isNot(AlarmStatus)
       */
      @Deprecated
      public AlertStatusesForAlertBuilder isNot(String value) {
        params.put(fieldName + "[is_not]", value);
        return builder;
      }

      /**
       * @deprecated This method accepting raw String will be removed in a future version. Use the
       *     type-safe enum overload instead:
       *     <pre>{@code .alarmStatus().in(AlarmStatus.VALUE1, AlarmStatus.VALUE2)}</pre>
       *
       * @see #in(AlarmStatus[])
       */
      @Deprecated
      public AlertStatusesForAlertBuilder in(String... values) {
        params.put(fieldName + "[in]", "[" + String.join(",", values) + "]");
        return builder;
      }

      /**
       * @deprecated This method accepting raw String will be removed in a future version. Use the
       *     type-safe enum overload instead:
       *     <pre>{@code .alarmStatus().notIn(AlarmStatus.VALUE1, AlarmStatus.VALUE2)}</pre>
       *
       * @see #notIn(AlarmStatus[])
       */
      @Deprecated
      public AlertStatusesForAlertBuilder notIn(String... values) {
        params.put(fieldName + "[not_in]", "[" + String.join(",", values) + "]");
        return builder;
      }
    }
  }

  public enum AlarmStatusIs {
    WITHIN_LIMIT("within_limit"),

    IN_ALARM("in_alarm"),

    /** An enum member indicating that AlarmStatusIs was instantiated with an unknown value. */
    _UNKNOWN(null);
    private final String value;

    AlarmStatusIs(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    public static AlarmStatusIs fromString(String value) {
      if (value == null) return _UNKNOWN;
      for (AlarmStatusIs enumValue : AlarmStatusIs.values()) {
        if (enumValue.value != null && enumValue.value.equals(value)) {
          return enumValue;
        }
      }
      return _UNKNOWN;
    }
  }

  public enum AlarmStatus {
    WITHIN_LIMIT("within_limit"),

    IN_ALARM("in_alarm"),

    /** An enum member indicating that AlarmStatus was instantiated with an unknown value. */
    _UNKNOWN(null);
    private final String value;

    AlarmStatus(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    public static AlarmStatus fromString(String value) {
      if (value == null) return _UNKNOWN;
      for (AlarmStatus enumValue : AlarmStatus.values()) {
        if (enumValue.value != null && enumValue.value.equals(value)) {
          return enumValue;
        }
      }
      return _UNKNOWN;
    }
  }
}
