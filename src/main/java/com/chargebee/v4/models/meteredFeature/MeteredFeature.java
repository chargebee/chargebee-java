/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.v4.models.meteredFeature;

import com.chargebee.v4.internal.JsonUtil;
import com.google.gson.JsonObject;
import java.sql.Timestamp;
import java.util.List;

public class MeteredFeature {

  private String id;
  private String name;
  private String description;
  private Type type;
  private Status status;
  private String query;
  private List<ColumnDefinitions> columnDefinitions;
  private List<Features> features;

  public String getId() {
    return id;
  }

  public String getName() {
    return name;
  }

  public String getDescription() {
    return description;
  }

  public Type getType() {
    return type;
  }

  public Status getStatus() {
    return status;
  }

  public String getQuery() {
    return query;
  }

  public List<ColumnDefinitions> getColumnDefinitions() {
    return columnDefinitions;
  }

  public List<Features> getFeatures() {
    return features;
  }

  public enum Type {
    SIMPLE("simple"),

    COMPOUND("compound"),

    /** An enum member indicating that Type was instantiated with an unknown value. */
    _UNKNOWN(null);
    private final String value;

    Type(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    public static Type fromString(String value) {
      if (value == null) return _UNKNOWN;
      for (Type enumValue : Type.values()) {
        if (enumValue.value != null && enumValue.value.equals(value)) {
          return enumValue;
        }
      }
      return _UNKNOWN;
    }
  }

  public enum Status {
    ACTIVE("active"),

    ARCHIVED("archived"),

    DELETED("deleted"),

    /** An enum member indicating that Status was instantiated with an unknown value. */
    _UNKNOWN(null);
    private final String value;

    Status(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    public static Status fromString(String value) {
      if (value == null) return _UNKNOWN;
      for (Status enumValue : Status.values()) {
        if (enumValue.value != null && enumValue.value.equals(value)) {
          return enumValue;
        }
      }
      return _UNKNOWN;
    }
  }

  public static MeteredFeature fromJson(String json) {
    return fromJson(JsonUtil.parse(json));
  }

  public static MeteredFeature fromJson(java.util.Map<String, Object> map) {
    return fromJson(JsonUtil.toJson(map));
  }

  public static MeteredFeature fromJson(JsonObject jsonObj) {
    MeteredFeature obj = new MeteredFeature();

    obj.id = JsonUtil.getString(jsonObj, "id");

    obj.name = JsonUtil.getString(jsonObj, "name");

    obj.description = JsonUtil.getString(jsonObj, "description");

    obj.type = Type.fromString(JsonUtil.getString(jsonObj, "type"));

    obj.status = Status.fromString(JsonUtil.getString(jsonObj, "status"));

    obj.query = JsonUtil.getString(jsonObj, "query");

    obj.columnDefinitions =
        JsonUtil.mapArray(
            JsonUtil.getJsonArray(jsonObj, "column_definitions"), ColumnDefinitions::fromJson);

    obj.features =
        JsonUtil.mapArray(JsonUtil.getJsonArray(jsonObj, "features"), Features::fromJson);

    return obj;
  }

  @Override
  public String toString() {
    return "MeteredFeature{"
        + "id="
        + id
        + ", name="
        + name
        + ", description="
        + description
        + ", type="
        + type
        + ", status="
        + status
        + ", query="
        + query
        + ", columnDefinitions="
        + columnDefinitions
        + ", features="
        + features
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) return true;
    if (o == null || getClass() != o.getClass()) return false;

    MeteredFeature that = (MeteredFeature) o;
    return java.util.Objects.equals(id, that.id)
        && java.util.Objects.equals(name, that.name)
        && java.util.Objects.equals(description, that.description)
        && java.util.Objects.equals(type, that.type)
        && java.util.Objects.equals(status, that.status)
        && java.util.Objects.equals(query, that.query)
        && java.util.Objects.equals(columnDefinitions, that.columnDefinitions)
        && java.util.Objects.equals(features, that.features);
  }

  @Override
  public int hashCode() {

    return java.util.Objects.hash(
        id, name, description, type, status, query, columnDefinitions, features);
  }

  public static class ColumnDefinitions {

    private String columnName;
    private DataType dataType;

    public String getColumnName() {
      return columnName;
    }

    public DataType getDataType() {
      return dataType;
    }

    public enum DataType {
      NUMBER("number"),

      STRING("string"),

      /** An enum member indicating that DataType was instantiated with an unknown value. */
      _UNKNOWN(null);
      private final String value;

      DataType(String value) {
        this.value = value;
      }

      public String getValue() {
        return value;
      }

      public static DataType fromString(String value) {
        if (value == null) return _UNKNOWN;
        for (DataType enumValue : DataType.values()) {
          if (enumValue.value != null && enumValue.value.equals(value)) {
            return enumValue;
          }
        }
        return _UNKNOWN;
      }
    }

    public static ColumnDefinitions fromJson(String json) {
      return fromJson(JsonUtil.parse(json));
    }

    public static ColumnDefinitions fromJson(java.util.Map<String, Object> map) {
      return fromJson(JsonUtil.toJson(map));
    }

    public static ColumnDefinitions fromJson(JsonObject jsonObj) {
      ColumnDefinitions obj = new ColumnDefinitions();

      obj.columnName = JsonUtil.getString(jsonObj, "column_name");

      obj.dataType = DataType.fromString(JsonUtil.getString(jsonObj, "data_type"));

      return obj;
    }

    @Override
    public String toString() {
      return "ColumnDefinitions{" + "columnName=" + columnName + ", dataType=" + dataType + "}";
    }

    @Override
    public boolean equals(Object o) {
      if (this == o) return true;
      if (o == null || getClass() != o.getClass()) return false;

      ColumnDefinitions that = (ColumnDefinitions) o;
      return java.util.Objects.equals(columnName, that.columnName)
          && java.util.Objects.equals(dataType, that.dataType);
    }

    @Override
    public int hashCode() {

      return java.util.Objects.hash(columnName, dataType);
    }
  }

  public static class Features {

    private String id;
    private String name;
    private String description;
    private Status status;
    private Type type;
    private String unit;
    private Long resourceVersion;
    private Timestamp updatedAt;
    private Timestamp createdAt;
    private Boolean metered;
    private List<Levels> levels;

    public String getId() {
      return id;
    }

    public String getName() {
      return name;
    }

    public String getDescription() {
      return description;
    }

    public Status getStatus() {
      return status;
    }

    public Type getType() {
      return type;
    }

    public String getUnit() {
      return unit;
    }

    public Long getResourceVersion() {
      return resourceVersion;
    }

    public Timestamp getUpdatedAt() {
      return updatedAt;
    }

    public Timestamp getCreatedAt() {
      return createdAt;
    }

    public Boolean getMetered() {
      return metered;
    }

    public List<Levels> getLevels() {
      return levels;
    }

    public enum Status {
      ACTIVE("active"),

      ARCHIVED("archived"),

      DRAFT("draft"),

      /** An enum member indicating that Status was instantiated with an unknown value. */
      _UNKNOWN(null);
      private final String value;

      Status(String value) {
        this.value = value;
      }

      public String getValue() {
        return value;
      }

      public static Status fromString(String value) {
        if (value == null) return _UNKNOWN;
        for (Status enumValue : Status.values()) {
          if (enumValue.value != null && enumValue.value.equals(value)) {
            return enumValue;
          }
        }
        return _UNKNOWN;
      }
    }

    public enum Type {
      SWITCH("switch"),

      CUSTOM("custom"),

      QUANTITY("quantity"),

      RANGE("range"),

      /** An enum member indicating that Type was instantiated with an unknown value. */
      _UNKNOWN(null);
      private final String value;

      Type(String value) {
        this.value = value;
      }

      public String getValue() {
        return value;
      }

      public static Type fromString(String value) {
        if (value == null) return _UNKNOWN;
        for (Type enumValue : Type.values()) {
          if (enumValue.value != null && enumValue.value.equals(value)) {
            return enumValue;
          }
        }
        return _UNKNOWN;
      }
    }

    public static Features fromJson(String json) {
      return fromJson(JsonUtil.parse(json));
    }

    public static Features fromJson(java.util.Map<String, Object> map) {
      return fromJson(JsonUtil.toJson(map));
    }

    public static Features fromJson(JsonObject jsonObj) {
      Features obj = new Features();

      obj.id = JsonUtil.getString(jsonObj, "id");

      obj.name = JsonUtil.getString(jsonObj, "name");

      obj.description = JsonUtil.getString(jsonObj, "description");

      obj.status = Status.fromString(JsonUtil.getString(jsonObj, "status"));

      obj.type = Type.fromString(JsonUtil.getString(jsonObj, "type"));

      obj.unit = JsonUtil.getString(jsonObj, "unit");

      obj.resourceVersion = JsonUtil.getLong(jsonObj, "resource_version");

      obj.updatedAt = JsonUtil.getTimestamp(jsonObj, "updated_at");

      obj.createdAt = JsonUtil.getTimestamp(jsonObj, "created_at");

      obj.metered = JsonUtil.getBoolean(jsonObj, "metered");

      obj.levels = JsonUtil.mapArray(JsonUtil.getJsonArray(jsonObj, "levels"), Levels::fromJson);

      return obj;
    }

    @Override
    public String toString() {
      return "Features{"
          + "id="
          + id
          + ", name="
          + name
          + ", description="
          + description
          + ", status="
          + status
          + ", type="
          + type
          + ", unit="
          + unit
          + ", resourceVersion="
          + resourceVersion
          + ", updatedAt="
          + updatedAt
          + ", createdAt="
          + createdAt
          + ", metered="
          + metered
          + ", levels="
          + levels
          + "}";
    }

    @Override
    public boolean equals(Object o) {
      if (this == o) return true;
      if (o == null || getClass() != o.getClass()) return false;

      Features that = (Features) o;
      return java.util.Objects.equals(id, that.id)
          && java.util.Objects.equals(name, that.name)
          && java.util.Objects.equals(description, that.description)
          && java.util.Objects.equals(status, that.status)
          && java.util.Objects.equals(type, that.type)
          && java.util.Objects.equals(unit, that.unit)
          && java.util.Objects.equals(resourceVersion, that.resourceVersion)
          && java.util.Objects.equals(updatedAt, that.updatedAt)
          && java.util.Objects.equals(createdAt, that.createdAt)
          && java.util.Objects.equals(metered, that.metered)
          && java.util.Objects.equals(levels, that.levels);
    }

    @Override
    public int hashCode() {

      return java.util.Objects.hash(
          id,
          name,
          description,
          status,
          type,
          unit,
          resourceVersion,
          updatedAt,
          createdAt,
          metered,
          levels);
    }

    public static class Levels {

      private String name;
      private String value;
      private Integer level;
      private Boolean isUnlimited;

      public String getName() {
        return name;
      }

      public String getValue() {
        return value;
      }

      public Integer getLevel() {
        return level;
      }

      public Boolean getIsUnlimited() {
        return isUnlimited;
      }

      public static Levels fromJson(String json) {
        return fromJson(JsonUtil.parse(json));
      }

      public static Levels fromJson(java.util.Map<String, Object> map) {
        return fromJson(JsonUtil.toJson(map));
      }

      public static Levels fromJson(JsonObject jsonObj) {
        Levels obj = new Levels();

        obj.name = JsonUtil.getString(jsonObj, "name");

        obj.value = JsonUtil.getString(jsonObj, "value");

        obj.level = JsonUtil.getInteger(jsonObj, "level");

        obj.isUnlimited = JsonUtil.getBoolean(jsonObj, "is_unlimited");

        return obj;
      }

      @Override
      public String toString() {
        return "Levels{"
            + "name="
            + name
            + ", value="
            + value
            + ", level="
            + level
            + ", isUnlimited="
            + isUnlimited
            + "}";
      }

      @Override
      public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        Levels that = (Levels) o;
        return java.util.Objects.equals(name, that.name)
            && java.util.Objects.equals(value, that.value)
            && java.util.Objects.equals(level, that.level)
            && java.util.Objects.equals(isUnlimited, that.isUnlimited);
      }

      @Override
      public int hashCode() {

        return java.util.Objects.hash(name, value, level, isUnlimited);
      }
    }
  }
}
