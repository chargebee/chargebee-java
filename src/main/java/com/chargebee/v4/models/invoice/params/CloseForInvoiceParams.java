/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */
package com.chargebee.v4.models.invoice.params;

import com.chargebee.v4.internal.Recommended;

import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.List;
import java.sql.Timestamp;

public final class CloseForInvoiceParams {

  private final String comment;

  private final String invoiceNote;

  private final Boolean removeGeneralNote;

  private final Timestamp invoiceDate;

  private final List<NotesToRemoveParams> notesToRemove;

  private final Map<String, Object> customFields;

  private CloseForInvoiceParams(CloseForInvoiceBuilder builder) {

    this.comment = builder.comment;

    this.invoiceNote = builder.invoiceNote;

    this.removeGeneralNote = builder.removeGeneralNote;

    this.invoiceDate = builder.invoiceDate;

    this.notesToRemove = builder.notesToRemove;

    this.customFields =
        builder.customFields.isEmpty()
            ? Collections.emptyMap()
            : Collections.unmodifiableMap(new LinkedHashMap<>(builder.customFields));
  }

  public String getComment() {
    return comment;
  }

  public String getInvoiceNote() {
    return invoiceNote;
  }

  public Boolean getRemoveGeneralNote() {
    return removeGeneralNote;
  }

  public Timestamp getInvoiceDate() {
    return invoiceDate;
  }

  public List<NotesToRemoveParams> getNotesToRemove() {
    return notesToRemove;
  }

  public Map<String, Object> customFields() {
    return customFields;
  }

  /** Get the form data for this request. */
  public Map<String, Object> toFormData() {
    Map<String, Object> formData = new LinkedHashMap<>();

    if (this.comment != null) {

      formData.put("comment", this.comment);
    }

    if (this.invoiceNote != null) {

      formData.put("invoice_note", this.invoiceNote);
    }

    if (this.removeGeneralNote != null) {

      formData.put("remove_general_note", this.removeGeneralNote);
    }

    if (this.invoiceDate != null) {

      formData.put("invoice_date", this.invoiceDate);
    }

    if (this.notesToRemove != null) {

      // List of objects
      for (int i = 0; i < this.notesToRemove.size(); i++) {
        NotesToRemoveParams item = this.notesToRemove.get(i);
        if (item != null) {
          Map<String, Object> itemData = item.toFormData();
          for (Map.Entry<String, Object> entry : itemData.entrySet()) {
            String indexedKey = "notes_to_remove[" + entry.getKey() + "][" + i + "]";
            formData.put(indexedKey, entry.getValue());
          }
        }
      }
    }

    formData.putAll(customFields);

    return formData;
  }

  /** Create a new builder for CloseForInvoiceParams. */
  @Recommended(reason = "Preferred for reusability, validation, and LLM-friendliness")
  public static CloseForInvoiceBuilder builder() {
    return new CloseForInvoiceBuilder();
  }

  public static final class CloseForInvoiceBuilder {

    private String comment;

    private String invoiceNote;

    private Boolean removeGeneralNote;

    private Timestamp invoiceDate;

    private List<NotesToRemoveParams> notesToRemove;

    private Map<String, Object> customFields = new LinkedHashMap<>();

    private CloseForInvoiceBuilder() {}

    public CloseForInvoiceBuilder comment(String value) {
      this.comment = value;
      return this;
    }

    public CloseForInvoiceBuilder invoiceNote(String value) {
      this.invoiceNote = value;
      return this;
    }

    public CloseForInvoiceBuilder removeGeneralNote(Boolean value) {
      this.removeGeneralNote = value;
      return this;
    }

    public CloseForInvoiceBuilder invoiceDate(Timestamp value) {
      this.invoiceDate = value;
      return this;
    }

    public CloseForInvoiceBuilder notesToRemove(List<NotesToRemoveParams> value) {
      this.notesToRemove = value;
      return this;
    }

    /**
     * Add a custom field to the request. Custom fields must start with "cf_".
     *
     * @param fieldName the name of the custom field (e.g., "cf_custom_field_name")
     * @param value the value of the custom field
     * @return this builder
     * @throws IllegalArgumentException if fieldName doesn't start with "cf_"
     */
    public CloseForInvoiceBuilder customField(String fieldName, Object value) {
      if (fieldName == null || !fieldName.startsWith("cf_")) {
        throw new IllegalArgumentException("Custom field name must start with 'cf_'");
      }
      this.customFields.put(fieldName, value);
      return this;
    }

    /**
     * Add multiple custom fields to the request. All field names must start with "cf_".
     *
     * @param customFields map of custom field names to values
     * @return this builder
     * @throws IllegalArgumentException if any field name doesn't start with "cf_"
     */
    public CloseForInvoiceBuilder customFields(Map<String, Object> customFields) {
      if (customFields != null) {
        for (Map.Entry<String, Object> entry : customFields.entrySet()) {
          if (entry.getKey() == null || !entry.getKey().startsWith("cf_")) {
            throw new IllegalArgumentException(
                "Custom field name must start with 'cf_': " + entry.getKey());
          }
          this.customFields.put(entry.getKey(), entry.getValue());
        }
      }
      return this;
    }

    public CloseForInvoiceParams build() {
      return new CloseForInvoiceParams(this);
    }
  }

  public static final class NotesToRemoveParams {

    private final EntityType entityType;

    private final String entityId;

    private NotesToRemoveParams(NotesToRemoveBuilder builder) {

      this.entityType = builder.entityType;

      this.entityId = builder.entityId;
    }

    public EntityType getEntityType() {
      return entityType;
    }

    public String getEntityId() {
      return entityId;
    }

    /** Get the form data for this request. */
    public Map<String, Object> toFormData() {
      Map<String, Object> formData = new LinkedHashMap<>();

      if (this.entityType != null) {

        formData.put("entity_type", this.entityType);
      }

      if (this.entityId != null) {

        formData.put("entity_id", this.entityId);
      }

      return formData;
    }

    /** Create a new builder for NotesToRemoveParams. */
    @Recommended(reason = "Preferred for reusability, validation, and LLM-friendliness")
    public static NotesToRemoveBuilder builder() {
      return new NotesToRemoveBuilder();
    }

    public static final class NotesToRemoveBuilder {

      private EntityType entityType;

      private String entityId;

      private NotesToRemoveBuilder() {}

      public NotesToRemoveBuilder entityType(EntityType value) {
        this.entityType = value;
        return this;
      }

      public NotesToRemoveBuilder entityId(String value) {
        this.entityId = value;
        return this;
      }

      public NotesToRemoveParams build() {
        return new NotesToRemoveParams(this);
      }
    }

    public enum EntityType {
      CUSTOMER("customer"),

      SUBSCRIPTION("subscription"),

      COUPON("coupon"),

      PLAN_ITEM_PRICE("plan_item_price"),

      ADDON_ITEM_PRICE("addon_item_price"),

      CHARGE_ITEM_PRICE("charge_item_price"),

      PLAN("plan"),

      ADDON("addon"),

      /** An enum member indicating that EntityType was instantiated with an unknown value. */
      _UNKNOWN(null);
      private final String value;

      EntityType(String value) {
        this.value = value;
      }

      public String getValue() {
        return value;
      }

      public static EntityType fromString(String value) {
        if (value == null) return _UNKNOWN;
        for (EntityType enumValue : EntityType.values()) {
          if (enumValue.value != null && enumValue.value.equals(value)) {
            return enumValue;
          }
        }
        return _UNKNOWN;
      }
    }
  }
}
