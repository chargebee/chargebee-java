/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.attachedItem.params.AttachedItemUpdateParams;

import com.chargebee.core.models.attachedItem.params.AttachedItemListParams;

import com.chargebee.core.models.attachedItem.params.AttachedItemCreateParams;

import com.chargebee.core.models.attachedItem.params.AttachedItemDeleteParams;

import com.chargebee.core.responses.attachedItem.AttachedItemRetrieveResponse;

import com.chargebee.core.responses.attachedItem.AttachedItemUpdateResponse;

import com.chargebee.core.responses.attachedItem.AttachedItemListResponse;

import com.chargebee.core.responses.attachedItem.AttachedItemCreateResponse;

import com.chargebee.core.responses.attachedItem.AttachedItemDeleteResponse;

public final class AttachedItemService extends BaseService<AttachedItemService> {

  private final ServiceConfig config;

  public AttachedItemService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private AttachedItemService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private AttachedItemService(
      ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  AttachedItemService with(RequestOptions newOptions) {
    return new AttachedItemService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public AttachedItemService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** retrieve a attachedItem (executes immediately) - returns raw Response. */
  Response retrieveRaw(String attachedItemId) throws Exception {
    String path =
        buildPathWithParams(
            "/attached_items/{attached-item-id}", "attached-item-id", attachedItemId);

    return get(path, null);
  }

  public AttachedItemRetrieveResponse retrieve(String attachedItemId) throws Exception {
    Response response = retrieveRaw(attachedItemId);
    return parseResponse(response, AttachedItemRetrieveResponse.class);
  }

  /** update a attachedItem (executes immediately) - returns raw Response. */
  Response updateRaw(String attachedItemId) throws Exception {
    String path =
        buildPathWithParams(
            "/attached_items/{attached-item-id}", "attached-item-id", attachedItemId);

    return post(path, null);
  }

  /** update a attachedItem using immutable params (executes immediately) - returns raw Response. */
  Response updateRaw(String attachedItemId, AttachedItemUpdateParams params) throws Exception {
    String path =
        buildPathWithParams(
            "/attached_items/{attached-item-id}", "attached-item-id", attachedItemId);
    return post(path, params.toFormData());
  }

  /** update a attachedItem using raw JSON payload (executes immediately) - returns raw Response. */
  Response updateRaw(String attachedItemId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/attached_items/{attached-item-id}", "attached-item-id", attachedItemId);
    return postJson(path, jsonPayload);
  }

  public AttachedItemUpdateResponse update(String attachedItemId, AttachedItemUpdateParams params)
      throws Exception {
    Response response = updateRaw(attachedItemId, params);
    return parseResponse(response, AttachedItemUpdateResponse.class);
  }

  /** list a attachedItem using immutable params (executes immediately) - returns raw Response. */
  Response listRaw(String itemId, AttachedItemListParams params) throws Exception {
    String path = buildPathWithParams("/items/{item-id}/attached_items", "item-id", itemId);
    return get(path, params != null ? params.toQueryParams() : null);
  }

  /** list a attachedItem without params (executes immediately) - returns raw Response. */
  Response listRaw(String itemId) throws Exception {
    String path = buildPathWithParams("/items/{item-id}/attached_items", "item-id", itemId);
    return get(path, null);
  }

  /** list a attachedItem using raw JSON payload (executes immediately) - returns raw Response. */
  Response listRaw(String itemId, String jsonPayload) throws Exception {
    String path = buildPathWithParams("/items/{item-id}/attached_items", "item-id", itemId);
    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public AttachedItemListResponse list(String itemId, AttachedItemListParams params)
      throws Exception {
    Response response = listRaw(itemId, params);
    return AttachedItemListResponse.fromJson(response.getBodyAsString(), this, params, itemId);
  }

  public AttachedItemListResponse list(String itemId) throws Exception {
    Response response = listRaw(itemId);
    return AttachedItemListResponse.fromJson(response.getBodyAsString(), this, null, itemId);
  }

  /** create a attachedItem (executes immediately) - returns raw Response. */
  Response createRaw(String itemId) throws Exception {
    String path = buildPathWithParams("/items/{item-id}/attached_items", "item-id", itemId);

    return post(path, null);
  }

  /** create a attachedItem using immutable params (executes immediately) - returns raw Response. */
  Response createRaw(String itemId, AttachedItemCreateParams params) throws Exception {
    String path = buildPathWithParams("/items/{item-id}/attached_items", "item-id", itemId);
    return post(path, params.toFormData());
  }

  /** create a attachedItem using raw JSON payload (executes immediately) - returns raw Response. */
  Response createRaw(String itemId, String jsonPayload) throws Exception {
    String path = buildPathWithParams("/items/{item-id}/attached_items", "item-id", itemId);
    return postJson(path, jsonPayload);
  }

  public AttachedItemCreateResponse create(String itemId, AttachedItemCreateParams params)
      throws Exception {
    Response response = createRaw(itemId, params);
    return parseResponse(response, AttachedItemCreateResponse.class);
  }

  /** delete a attachedItem (executes immediately) - returns raw Response. */
  Response deleteRaw(String attachedItemId) throws Exception {
    String path =
        buildPathWithParams(
            "/attached_items/{attached-item-id}/delete", "attached-item-id", attachedItemId);

    return post(path, null);
  }

  /** delete a attachedItem using immutable params (executes immediately) - returns raw Response. */
  Response deleteRaw(String attachedItemId, AttachedItemDeleteParams params) throws Exception {
    String path =
        buildPathWithParams(
            "/attached_items/{attached-item-id}/delete", "attached-item-id", attachedItemId);
    return post(path, params.toFormData());
  }

  /** delete a attachedItem using raw JSON payload (executes immediately) - returns raw Response. */
  Response deleteRaw(String attachedItemId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/attached_items/{attached-item-id}/delete", "attached-item-id", attachedItemId);
    return postJson(path, jsonPayload);
  }

  public AttachedItemDeleteResponse delete(String attachedItemId, AttachedItemDeleteParams params)
      throws Exception {
    Response response = deleteRaw(attachedItemId, params);
    return parseResponse(response, AttachedItemDeleteResponse.class);
  }
}
