/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.itemPrice.params.ItemPriceFindApplicableItemsParams;

import com.chargebee.core.models.itemPrice.params.ItemPriceUpdateParams;

import com.chargebee.core.models.itemPrice.params.ItemPriceFindApplicableItemPricesParams;

import com.chargebee.core.models.itemPrice.params.ItemPriceListParams;

import com.chargebee.core.models.itemPrice.params.ItemPriceCreateParams;

import com.chargebee.core.responses.itemPrice.ItemPriceFindApplicableItemsResponse;

import com.chargebee.core.responses.itemPrice.ItemPriceRetrieveResponse;

import com.chargebee.core.responses.itemPrice.ItemPriceUpdateResponse;

import com.chargebee.core.responses.itemPrice.ItemPriceDeleteResponse;

import com.chargebee.core.responses.itemPrice.ItemPriceFindApplicableItemPricesResponse;

import com.chargebee.core.responses.itemPrice.ItemPriceListResponse;

import com.chargebee.core.responses.itemPrice.ItemPriceCreateResponse;

public final class ItemPriceService extends BaseService<ItemPriceService> {

  private final ServiceConfig config;

  public ItemPriceService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private ItemPriceService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private ItemPriceService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  ItemPriceService with(RequestOptions newOptions) {
    return new ItemPriceService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public ItemPriceService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /**
   * findApplicableItems a itemPrice using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response findApplicableItemsRaw(String itemPriceId, ItemPriceFindApplicableItemsParams params)
      throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/applicable_items", "item-price-id", itemPriceId);
    return get(path, params != null ? params.toQueryParams() : null);
  }

  /**
   * findApplicableItems a itemPrice without params (executes immediately) - returns raw Response.
   */
  Response findApplicableItemsRaw(String itemPriceId) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/applicable_items", "item-price-id", itemPriceId);
    return get(path, null);
  }

  /**
   * findApplicableItems a itemPrice using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response findApplicableItemsRaw(String itemPriceId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/applicable_items", "item-price-id", itemPriceId);
    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public ItemPriceFindApplicableItemsResponse findApplicableItems(
      String itemPriceId, ItemPriceFindApplicableItemsParams params) throws Exception {
    Response response = findApplicableItemsRaw(itemPriceId, params);
    return ItemPriceFindApplicableItemsResponse.fromJson(
        response.getBodyAsString(), this, params, itemPriceId);
  }

  public ItemPriceFindApplicableItemsResponse findApplicableItems(String itemPriceId)
      throws Exception {
    Response response = findApplicableItemsRaw(itemPriceId);
    return ItemPriceFindApplicableItemsResponse.fromJson(
        response.getBodyAsString(), this, null, itemPriceId);
  }

  /** retrieve a itemPrice (executes immediately) - returns raw Response. */
  Response retrieveRaw(String itemPriceId) throws Exception {
    String path = buildPathWithParams("/item_prices/{item-price-id}", "item-price-id", itemPriceId);

    return get(path, null);
  }

  public ItemPriceRetrieveResponse retrieve(String itemPriceId) throws Exception {
    Response response = retrieveRaw(itemPriceId);
    return parseResponse(response, ItemPriceRetrieveResponse.class);
  }

  /** update a itemPrice (executes immediately) - returns raw Response. */
  Response updateRaw(String itemPriceId) throws Exception {
    String path = buildPathWithParams("/item_prices/{item-price-id}", "item-price-id", itemPriceId);

    return post(path, null);
  }

  /** update a itemPrice using immutable params (executes immediately) - returns raw Response. */
  Response updateRaw(String itemPriceId, ItemPriceUpdateParams params) throws Exception {
    String path = buildPathWithParams("/item_prices/{item-price-id}", "item-price-id", itemPriceId);
    return post(path, params.toFormData());
  }

  /** update a itemPrice using raw JSON payload (executes immediately) - returns raw Response. */
  Response updateRaw(String itemPriceId, String jsonPayload) throws Exception {
    String path = buildPathWithParams("/item_prices/{item-price-id}", "item-price-id", itemPriceId);
    return postJson(path, jsonPayload);
  }

  public ItemPriceUpdateResponse update(String itemPriceId, ItemPriceUpdateParams params)
      throws Exception {
    Response response = updateRaw(itemPriceId, params);
    return parseResponse(response, ItemPriceUpdateResponse.class);
  }

  /** delete a itemPrice (executes immediately) - returns raw Response. */
  Response deleteRaw(String itemPriceId) throws Exception {
    String path =
        buildPathWithParams("/item_prices/{item-price-id}/delete", "item-price-id", itemPriceId);

    return post(path, null);
  }

  public ItemPriceDeleteResponse delete(String itemPriceId) throws Exception {
    Response response = deleteRaw(itemPriceId);
    return parseResponse(response, ItemPriceDeleteResponse.class);
  }

  /**
   * findApplicableItemPrices a itemPrice using immutable params (executes immediately) - returns
   * raw Response.
   */
  Response findApplicableItemPricesRaw(
      String itemPriceId, ItemPriceFindApplicableItemPricesParams params) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/applicable_item_prices", "item-price-id", itemPriceId);
    return get(path, params != null ? params.toQueryParams() : null);
  }

  /**
   * findApplicableItemPrices a itemPrice without params (executes immediately) - returns raw
   * Response.
   */
  Response findApplicableItemPricesRaw(String itemPriceId) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/applicable_item_prices", "item-price-id", itemPriceId);
    return get(path, null);
  }

  /**
   * findApplicableItemPrices a itemPrice using raw JSON payload (executes immediately) - returns
   * raw Response.
   */
  Response findApplicableItemPricesRaw(String itemPriceId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/applicable_item_prices", "item-price-id", itemPriceId);
    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public ItemPriceFindApplicableItemPricesResponse findApplicableItemPrices(
      String itemPriceId, ItemPriceFindApplicableItemPricesParams params) throws Exception {
    Response response = findApplicableItemPricesRaw(itemPriceId, params);
    return ItemPriceFindApplicableItemPricesResponse.fromJson(
        response.getBodyAsString(), this, params, itemPriceId);
  }

  public ItemPriceFindApplicableItemPricesResponse findApplicableItemPrices(String itemPriceId)
      throws Exception {
    Response response = findApplicableItemPricesRaw(itemPriceId);
    return ItemPriceFindApplicableItemPricesResponse.fromJson(
        response.getBodyAsString(), this, null, itemPriceId);
  }

  /** list a itemPrice using immutable params (executes immediately) - returns raw Response. */
  Response listRaw(ItemPriceListParams params) throws Exception {

    return get("/item_prices", params != null ? params.toQueryParams() : null);
  }

  /** list a itemPrice without params (executes immediately) - returns raw Response. */
  Response listRaw() throws Exception {

    return get("/item_prices", null);
  }

  /** list a itemPrice using raw JSON payload (executes immediately) - returns raw Response. */
  Response listRaw(String jsonPayload) throws Exception {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public ItemPriceListResponse list(ItemPriceListParams params) throws Exception {
    Response response = listRaw(params);

    return ItemPriceListResponse.fromJson(response.getBodyAsString(), this, params);
  }

  public ItemPriceListResponse list() throws Exception {
    Response response = listRaw();
    return ItemPriceListResponse.fromJson(response.getBodyAsString(), this, null);
  }

  /** create a itemPrice using immutable params (executes immediately) - returns raw Response. */
  Response createRaw(ItemPriceCreateParams params) throws Exception {

    return post("/item_prices", params != null ? params.toFormData() : null);
  }

  /** create a itemPrice using raw JSON payload (executes immediately) - returns raw Response. */
  Response createRaw(String jsonPayload) throws Exception {

    return postJson("/item_prices", jsonPayload);
  }

  public ItemPriceCreateResponse create(ItemPriceCreateParams params) throws Exception {
    Response response = createRaw(params);

    return parseResponse(response, ItemPriceCreateResponse.class);
  }
}
