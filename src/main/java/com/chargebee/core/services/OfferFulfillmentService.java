/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.offerFulfillment.params.OfferFulfillmentOfferFulfillmentsParams;

import com.chargebee.core.models.offerFulfillment.params.OfferFulfillmentOfferFulfillmentsUpdateParams;

import com.chargebee.core.responses.offerFulfillment.OfferFulfillmentOfferFulfillmentsResponse;

import com.chargebee.core.responses.offerFulfillment.OfferFulfillmentOfferFulfillmentsGetResponse;

import com.chargebee.core.responses.offerFulfillment.OfferFulfillmentOfferFulfillmentsUpdateResponse;

public final class OfferFulfillmentService extends BaseService<OfferFulfillmentService> {

  private final ServiceConfig config;

  public OfferFulfillmentService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private OfferFulfillmentService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private OfferFulfillmentService(
      ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  OfferFulfillmentService with(RequestOptions newOptions) {
    return new OfferFulfillmentService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public OfferFulfillmentService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /**
   * offerFulfillments a offerFulfillment using immutable params (executes immediately) - returns
   * raw Response.
   */
  Response offerFulfillmentsRaw(OfferFulfillmentOfferFulfillmentsParams params) throws Exception {

    return post("/offer_fulfillments", params != null ? params.toFormData() : null);
  }

  /**
   * offerFulfillments a offerFulfillment using raw JSON payload (executes immediately) - returns
   * raw Response.
   */
  Response offerFulfillmentsRaw(String jsonPayload) throws Exception {

    return postJson("/offer_fulfillments", jsonPayload);
  }

  public OfferFulfillmentOfferFulfillmentsResponse offerFulfillments(
      OfferFulfillmentOfferFulfillmentsParams params) throws Exception {
    Response response = offerFulfillmentsRaw(params);

    return parseResponse(response, OfferFulfillmentOfferFulfillmentsResponse.class);
  }

  /** offerFulfillmentsGet a offerFulfillment (executes immediately) - returns raw Response. */
  Response offerFulfillmentsGetRaw(String offerFulfillmentId) throws Exception {
    String path =
        buildPathWithParams(
            "/offer_fulfillments/{offer-fulfillment-id}",
            "offer-fulfillment-id",
            offerFulfillmentId);

    return get(path, null);
  }

  public OfferFulfillmentOfferFulfillmentsGetResponse offerFulfillmentsGet(
      String offerFulfillmentId) throws Exception {
    Response response = offerFulfillmentsGetRaw(offerFulfillmentId);
    return parseResponse(response, OfferFulfillmentOfferFulfillmentsGetResponse.class);
  }

  /** offerFulfillmentsUpdate a offerFulfillment (executes immediately) - returns raw Response. */
  Response offerFulfillmentsUpdateRaw(String offerFulfillmentId) throws Exception {
    String path =
        buildPathWithParams(
            "/offer_fulfillments/{offer-fulfillment-id}",
            "offer-fulfillment-id",
            offerFulfillmentId);

    return post(path, null);
  }

  /**
   * offerFulfillmentsUpdate a offerFulfillment using immutable params (executes immediately) -
   * returns raw Response.
   */
  Response offerFulfillmentsUpdateRaw(
      String offerFulfillmentId, OfferFulfillmentOfferFulfillmentsUpdateParams params)
      throws Exception {
    String path =
        buildPathWithParams(
            "/offer_fulfillments/{offer-fulfillment-id}",
            "offer-fulfillment-id",
            offerFulfillmentId);
    return post(path, params.toFormData());
  }

  /**
   * offerFulfillmentsUpdate a offerFulfillment using raw JSON payload (executes immediately) -
   * returns raw Response.
   */
  Response offerFulfillmentsUpdateRaw(String offerFulfillmentId, String jsonPayload)
      throws Exception {
    String path =
        buildPathWithParams(
            "/offer_fulfillments/{offer-fulfillment-id}",
            "offer-fulfillment-id",
            offerFulfillmentId);
    return postJson(path, jsonPayload);
  }

  public OfferFulfillmentOfferFulfillmentsUpdateResponse offerFulfillmentsUpdate(
      String offerFulfillmentId, OfferFulfillmentOfferFulfillmentsUpdateParams params)
      throws Exception {
    Response response = offerFulfillmentsUpdateRaw(offerFulfillmentId, params);
    return parseResponse(response, OfferFulfillmentOfferFulfillmentsUpdateResponse.class);
  }
}
