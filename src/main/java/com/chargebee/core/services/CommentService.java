/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.comment.params.CommentListParams;

import com.chargebee.core.models.comment.params.CommentCreateParams;

import com.chargebee.core.responses.comment.CommentDeleteResponse;

import com.chargebee.core.responses.comment.CommentRetrieveResponse;

import com.chargebee.core.responses.comment.CommentListResponse;

import com.chargebee.core.responses.comment.CommentCreateResponse;

public final class CommentService extends BaseService<CommentService> {

  private final ServiceConfig config;

  public CommentService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private CommentService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private CommentService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  CommentService with(RequestOptions newOptions) {
    return new CommentService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public CommentService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** delete a comment (executes immediately) - returns raw Response. */
  Response deleteRaw(String commentId) throws Exception {
    String path = buildPathWithParams("/comments/{comment-id}/delete", "comment-id", commentId);

    return post(path, null);
  }

  public CommentDeleteResponse delete(String commentId) throws Exception {
    Response response = deleteRaw(commentId);
    return parseResponse(response, CommentDeleteResponse.class);
  }

  /** retrieve a comment (executes immediately) - returns raw Response. */
  Response retrieveRaw(String commentId) throws Exception {
    String path = buildPathWithParams("/comments/{comment-id}", "comment-id", commentId);

    return get(path, null);
  }

  public CommentRetrieveResponse retrieve(String commentId) throws Exception {
    Response response = retrieveRaw(commentId);
    return parseResponse(response, CommentRetrieveResponse.class);
  }

  /** list a comment using immutable params (executes immediately) - returns raw Response. */
  Response listRaw(CommentListParams params) throws Exception {

    return get("/comments", params != null ? params.toQueryParams() : null);
  }

  /** list a comment without params (executes immediately) - returns raw Response. */
  Response listRaw() throws Exception {

    return get("/comments", null);
  }

  /** list a comment using raw JSON payload (executes immediately) - returns raw Response. */
  Response listRaw(String jsonPayload) throws Exception {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public CommentListResponse list(CommentListParams params) throws Exception {
    Response response = listRaw(params);

    return CommentListResponse.fromJson(response.getBodyAsString(), this, params);
  }

  public CommentListResponse list() throws Exception {
    Response response = listRaw();
    return CommentListResponse.fromJson(response.getBodyAsString(), this, null);
  }

  /** create a comment using immutable params (executes immediately) - returns raw Response. */
  Response createRaw(CommentCreateParams params) throws Exception {

    return post("/comments", params != null ? params.toFormData() : null);
  }

  /** create a comment using raw JSON payload (executes immediately) - returns raw Response. */
  Response createRaw(String jsonPayload) throws Exception {

    return postJson("/comments", jsonPayload);
  }

  public CommentCreateResponse create(CommentCreateParams params) throws Exception {
    Response response = createRaw(params);

    return parseResponse(response, CommentCreateResponse.class);
  }
}
