/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.usageEvent.params.UsageEventCreateParams;

import com.chargebee.core.models.usageEvent.params.UsageEventBatchIngestParams;

import com.chargebee.core.responses.usageEvent.UsageEventCreateResponse;

import com.chargebee.core.responses.usageEvent.UsageEventBatchIngestResponse;

public final class UsageEventService extends BaseService<UsageEventService> {

  private final ServiceConfig config;

  public UsageEventService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private UsageEventService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private UsageEventService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  UsageEventService with(RequestOptions newOptions) {
    return new UsageEventService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public UsageEventService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** create a usageEvent using immutable params (executes immediately) - returns raw Response. */
  Response createRaw(UsageEventCreateParams params) throws Exception {

    return post("/usage_events", params != null ? params.toFormData() : null);
  }

  /** create a usageEvent using raw JSON payload (executes immediately) - returns raw Response. */
  Response createRaw(String jsonPayload) throws Exception {

    return postJson("/usage_events", jsonPayload);
  }

  public UsageEventCreateResponse create(UsageEventCreateParams params) throws Exception {
    Response response = createRaw(params);

    return parseResponse(response, UsageEventCreateResponse.class);
  }

  /**
   * batchIngest a usageEvent using immutable params (executes immediately) - returns raw Response.
   */
  Response batchIngestRaw(UsageEventBatchIngestParams params) throws Exception {

    return post("/batch/usage_events", params != null ? params.toFormData() : null);
  }

  /**
   * batchIngest a usageEvent using raw JSON payload (executes immediately) - returns raw Response.
   */
  Response batchIngestRaw(String jsonPayload) throws Exception {

    return postJson("/batch/usage_events", jsonPayload);
  }

  public UsageEventBatchIngestResponse batchIngest(UsageEventBatchIngestParams params)
      throws Exception {
    Response response = batchIngestRaw(params);

    return parseResponse(response, UsageEventBatchIngestResponse.class);
  }
}
