/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.creditNote.params.CreditNoteVoidCreditNoteParams;

import com.chargebee.core.models.creditNote.params.CreditNoteRefundParams;

import com.chargebee.core.models.creditNote.params.CreditNoteListParams;

import com.chargebee.core.models.creditNote.params.CreditNoteCreateParams;

import com.chargebee.core.models.creditNote.params.CreditNoteRecordRefundParams;

import com.chargebee.core.models.creditNote.params.CreditNoteImportCreditNoteParams;

import com.chargebee.core.models.creditNote.params.CreditNoteDeleteParams;

import com.chargebee.core.models.creditNote.params.CreditNoteCreditNotesForCustomerParams;

import com.chargebee.core.models.creditNote.params.CreditNotePdfParams;

import com.chargebee.core.models.creditNote.params.CreditNoteRemoveTaxWithheldRefundParams;

import com.chargebee.core.responses.creditNote.CreditNoteVoidCreditNoteResponse;

import com.chargebee.core.responses.creditNote.CreditNoteRefundResponse;

import com.chargebee.core.responses.creditNote.CreditNoteListResponse;

import com.chargebee.core.responses.creditNote.CreditNoteCreateResponse;

import com.chargebee.core.responses.creditNote.CreditNoteRecordRefundResponse;

import com.chargebee.core.responses.creditNote.CreditNoteImportCreditNoteResponse;

import com.chargebee.core.responses.creditNote.CreditNoteDeleteResponse;

import com.chargebee.core.responses.creditNote.CreditNoteCreditNotesForCustomerResponse;

import com.chargebee.core.responses.creditNote.CreditNoteDownloadEinvoiceResponse;

import com.chargebee.core.responses.creditNote.CreditNotePdfResponse;

import com.chargebee.core.responses.creditNote.CreditNoteResendEinvoiceResponse;

import com.chargebee.core.responses.creditNote.CreditNoteRemoveTaxWithheldRefundResponse;

import com.chargebee.core.responses.creditNote.CreditNoteRetrieveResponse;

import com.chargebee.core.responses.creditNote.CreditNoteSendEinvoiceResponse;

public final class CreditNoteService extends BaseService<CreditNoteService> {

  private final ServiceConfig config;

  public CreditNoteService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private CreditNoteService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private CreditNoteService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  CreditNoteService with(RequestOptions newOptions) {
    return new CreditNoteService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public CreditNoteService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** voidCreditNote a creditNote (executes immediately) - returns raw Response. */
  Response voidCreditNoteRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams("/credit_notes/{credit-note-id}/void", "credit-note-id", creditNoteId);

    return post(path, null);
  }

  /**
   * voidCreditNote a creditNote using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response voidCreditNoteRaw(String creditNoteId, CreditNoteVoidCreditNoteParams params)
      throws Exception {
    String path =
        buildPathWithParams("/credit_notes/{credit-note-id}/void", "credit-note-id", creditNoteId);
    return post(path, params.toFormData());
  }

  /**
   * voidCreditNote a creditNote using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response voidCreditNoteRaw(String creditNoteId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams("/credit_notes/{credit-note-id}/void", "credit-note-id", creditNoteId);
    return postJson(path, jsonPayload);
  }

  public CreditNoteVoidCreditNoteResponse voidCreditNote(
      String creditNoteId, CreditNoteVoidCreditNoteParams params) throws Exception {
    Response response = voidCreditNoteRaw(creditNoteId, params);
    return parseResponse(response, CreditNoteVoidCreditNoteResponse.class);
  }

  /** refund a creditNote (executes immediately) - returns raw Response. */
  Response refundRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/refund", "credit-note-id", creditNoteId);

    return post(path, null);
  }

  /** refund a creditNote using immutable params (executes immediately) - returns raw Response. */
  Response refundRaw(String creditNoteId, CreditNoteRefundParams params) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/refund", "credit-note-id", creditNoteId);
    return post(path, params.toFormData());
  }

  /** refund a creditNote using raw JSON payload (executes immediately) - returns raw Response. */
  Response refundRaw(String creditNoteId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/refund", "credit-note-id", creditNoteId);
    return postJson(path, jsonPayload);
  }

  public CreditNoteRefundResponse refund(String creditNoteId, CreditNoteRefundParams params)
      throws Exception {
    Response response = refundRaw(creditNoteId, params);
    return parseResponse(response, CreditNoteRefundResponse.class);
  }

  /** list a creditNote using immutable params (executes immediately) - returns raw Response. */
  Response listRaw(CreditNoteListParams params) throws Exception {

    return get("/credit_notes", params != null ? params.toQueryParams() : null);
  }

  /** list a creditNote without params (executes immediately) - returns raw Response. */
  Response listRaw() throws Exception {

    return get("/credit_notes", null);
  }

  /** list a creditNote using raw JSON payload (executes immediately) - returns raw Response. */
  Response listRaw(String jsonPayload) throws Exception {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public CreditNoteListResponse list(CreditNoteListParams params) throws Exception {
    Response response = listRaw(params);

    return CreditNoteListResponse.fromJson(response.getBodyAsString(), this, params);
  }

  public CreditNoteListResponse list() throws Exception {
    Response response = listRaw();
    return CreditNoteListResponse.fromJson(response.getBodyAsString(), this, null);
  }

  /** create a creditNote using immutable params (executes immediately) - returns raw Response. */
  Response createRaw(CreditNoteCreateParams params) throws Exception {

    return post("/credit_notes", params != null ? params.toFormData() : null);
  }

  /** create a creditNote using raw JSON payload (executes immediately) - returns raw Response. */
  Response createRaw(String jsonPayload) throws Exception {

    return postJson("/credit_notes", jsonPayload);
  }

  public CreditNoteCreateResponse create(CreditNoteCreateParams params) throws Exception {
    Response response = createRaw(params);

    return parseResponse(response, CreditNoteCreateResponse.class);
  }

  /** recordRefund a creditNote (executes immediately) - returns raw Response. */
  Response recordRefundRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/record_refund", "credit-note-id", creditNoteId);

    return post(path, null);
  }

  /**
   * recordRefund a creditNote using immutable params (executes immediately) - returns raw Response.
   */
  Response recordRefundRaw(String creditNoteId, CreditNoteRecordRefundParams params)
      throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/record_refund", "credit-note-id", creditNoteId);
    return post(path, params.toFormData());
  }

  /**
   * recordRefund a creditNote using raw JSON payload (executes immediately) - returns raw Response.
   */
  Response recordRefundRaw(String creditNoteId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/record_refund", "credit-note-id", creditNoteId);
    return postJson(path, jsonPayload);
  }

  public CreditNoteRecordRefundResponse recordRefund(
      String creditNoteId, CreditNoteRecordRefundParams params) throws Exception {
    Response response = recordRefundRaw(creditNoteId, params);
    return parseResponse(response, CreditNoteRecordRefundResponse.class);
  }

  /**
   * importCreditNote a creditNote using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response importCreditNoteRaw(CreditNoteImportCreditNoteParams params) throws Exception {

    return post("/credit_notes/import_credit_note", params != null ? params.toFormData() : null);
  }

  /**
   * importCreditNote a creditNote using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response importCreditNoteRaw(String jsonPayload) throws Exception {

    return postJson("/credit_notes/import_credit_note", jsonPayload);
  }

  public CreditNoteImportCreditNoteResponse importCreditNote(
      CreditNoteImportCreditNoteParams params) throws Exception {
    Response response = importCreditNoteRaw(params);

    return parseResponse(response, CreditNoteImportCreditNoteResponse.class);
  }

  /** delete a creditNote (executes immediately) - returns raw Response. */
  Response deleteRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/delete", "credit-note-id", creditNoteId);

    return post(path, null);
  }

  /** delete a creditNote using immutable params (executes immediately) - returns raw Response. */
  Response deleteRaw(String creditNoteId, CreditNoteDeleteParams params) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/delete", "credit-note-id", creditNoteId);
    return post(path, params.toFormData());
  }

  /** delete a creditNote using raw JSON payload (executes immediately) - returns raw Response. */
  Response deleteRaw(String creditNoteId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/delete", "credit-note-id", creditNoteId);
    return postJson(path, jsonPayload);
  }

  public CreditNoteDeleteResponse delete(String creditNoteId, CreditNoteDeleteParams params)
      throws Exception {
    Response response = deleteRaw(creditNoteId, params);
    return parseResponse(response, CreditNoteDeleteResponse.class);
  }

  /**
   * creditNotesForCustomer a creditNote using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response creditNotesForCustomerRaw(
      String customerId, CreditNoteCreditNotesForCustomerParams params) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/credit_notes", "customer-id", customerId);
    return get(path, params != null ? params.toQueryParams() : null);
  }

  /**
   * creditNotesForCustomer a creditNote without params (executes immediately) - returns raw
   * Response.
   */
  Response creditNotesForCustomerRaw(String customerId) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/credit_notes", "customer-id", customerId);
    return get(path, null);
  }

  /**
   * creditNotesForCustomer a creditNote using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response creditNotesForCustomerRaw(String customerId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/credit_notes", "customer-id", customerId);
    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public CreditNoteCreditNotesForCustomerResponse creditNotesForCustomer(
      String customerId, CreditNoteCreditNotesForCustomerParams params) throws Exception {
    Response response = creditNotesForCustomerRaw(customerId, params);
    return CreditNoteCreditNotesForCustomerResponse.fromJson(
        response.getBodyAsString(), this, params, customerId);
  }

  public CreditNoteCreditNotesForCustomerResponse creditNotesForCustomer(String customerId)
      throws Exception {
    Response response = creditNotesForCustomerRaw(customerId);
    return CreditNoteCreditNotesForCustomerResponse.fromJson(
        response.getBodyAsString(), this, null, customerId);
  }

  /** downloadEinvoice a creditNote (executes immediately) - returns raw Response. */
  Response downloadEinvoiceRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/download_einvoice", "credit-note-id", creditNoteId);

    return get(path, null);
  }

  public CreditNoteDownloadEinvoiceResponse downloadEinvoice(String creditNoteId) throws Exception {
    Response response = downloadEinvoiceRaw(creditNoteId);
    return parseResponse(response, CreditNoteDownloadEinvoiceResponse.class);
  }

  /** pdf a creditNote (executes immediately) - returns raw Response. */
  Response pdfRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams("/credit_notes/{credit-note-id}/pdf", "credit-note-id", creditNoteId);

    return post(path, null);
  }

  /** pdf a creditNote using immutable params (executes immediately) - returns raw Response. */
  Response pdfRaw(String creditNoteId, CreditNotePdfParams params) throws Exception {
    String path =
        buildPathWithParams("/credit_notes/{credit-note-id}/pdf", "credit-note-id", creditNoteId);
    return post(path, params.toFormData());
  }

  /** pdf a creditNote using raw JSON payload (executes immediately) - returns raw Response. */
  Response pdfRaw(String creditNoteId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams("/credit_notes/{credit-note-id}/pdf", "credit-note-id", creditNoteId);
    return postJson(path, jsonPayload);
  }

  public CreditNotePdfResponse pdf(String creditNoteId, CreditNotePdfParams params)
      throws Exception {
    Response response = pdfRaw(creditNoteId, params);
    return parseResponse(response, CreditNotePdfResponse.class);
  }

  /** resendEinvoice a creditNote (executes immediately) - returns raw Response. */
  Response resendEinvoiceRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/resend_einvoice", "credit-note-id", creditNoteId);

    return post(path, null);
  }

  public CreditNoteResendEinvoiceResponse resendEinvoice(String creditNoteId) throws Exception {
    Response response = resendEinvoiceRaw(creditNoteId);
    return parseResponse(response, CreditNoteResendEinvoiceResponse.class);
  }

  /** removeTaxWithheldRefund a creditNote (executes immediately) - returns raw Response. */
  Response removeTaxWithheldRefundRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/remove_tax_withheld_refund",
            "credit-note-id",
            creditNoteId);

    return post(path, null);
  }

  /**
   * removeTaxWithheldRefund a creditNote using immutable params (executes immediately) - returns
   * raw Response.
   */
  Response removeTaxWithheldRefundRaw(
      String creditNoteId, CreditNoteRemoveTaxWithheldRefundParams params) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/remove_tax_withheld_refund",
            "credit-note-id",
            creditNoteId);
    return post(path, params.toFormData());
  }

  /**
   * removeTaxWithheldRefund a creditNote using raw JSON payload (executes immediately) - returns
   * raw Response.
   */
  Response removeTaxWithheldRefundRaw(String creditNoteId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/remove_tax_withheld_refund",
            "credit-note-id",
            creditNoteId);
    return postJson(path, jsonPayload);
  }

  public CreditNoteRemoveTaxWithheldRefundResponse removeTaxWithheldRefund(
      String creditNoteId, CreditNoteRemoveTaxWithheldRefundParams params) throws Exception {
    Response response = removeTaxWithheldRefundRaw(creditNoteId, params);
    return parseResponse(response, CreditNoteRemoveTaxWithheldRefundResponse.class);
  }

  /** retrieve a creditNote (executes immediately) - returns raw Response. */
  Response retrieveRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams("/credit_notes/{credit-note-id}", "credit-note-id", creditNoteId);

    return get(path, null);
  }

  public CreditNoteRetrieveResponse retrieve(String creditNoteId) throws Exception {
    Response response = retrieveRaw(creditNoteId);
    return parseResponse(response, CreditNoteRetrieveResponse.class);
  }

  /** sendEinvoice a creditNote (executes immediately) - returns raw Response. */
  Response sendEinvoiceRaw(String creditNoteId) throws Exception {
    String path =
        buildPathWithParams(
            "/credit_notes/{credit-note-id}/send_einvoice", "credit-note-id", creditNoteId);

    return post(path, null);
  }

  public CreditNoteSendEinvoiceResponse sendEinvoice(String creditNoteId) throws Exception {
    Response response = sendEinvoiceRaw(creditNoteId);
    return parseResponse(response, CreditNoteSendEinvoiceResponse.class);
  }
}
