/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.card.params.CardCopyCardForCustomerParams;

import com.chargebee.core.models.card.params.CardSwitchGatewayForCustomerParams;

import com.chargebee.core.models.card.params.CardUpdateCardForCustomerParams;

import com.chargebee.core.responses.card.CardCopyCardForCustomerResponse;

import com.chargebee.core.responses.card.CardRetrieveResponse;

import com.chargebee.core.responses.card.CardSwitchGatewayForCustomerResponse;

import com.chargebee.core.responses.card.CardDeleteCardForCustomerResponse;

import com.chargebee.core.responses.card.CardUpdateCardForCustomerResponse;

public final class CardService extends BaseService<CardService> {

  private final ServiceConfig config;

  public CardService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private CardService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private CardService(ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  CardService with(RequestOptions newOptions) {
    return new CardService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public CardService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** copyCardForCustomer a card (executes immediately) - returns raw Response. */
  Response copyCardForCustomerRaw(String customerId) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/copy_card", "customer-id", customerId);

    return post(path, null);
  }

  /**
   * copyCardForCustomer a card using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response copyCardForCustomerRaw(String customerId, CardCopyCardForCustomerParams params)
      throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/copy_card", "customer-id", customerId);
    return post(path, params.toFormData());
  }

  /**
   * copyCardForCustomer a card using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response copyCardForCustomerRaw(String customerId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/copy_card", "customer-id", customerId);
    return postJson(path, jsonPayload);
  }

  public CardCopyCardForCustomerResponse copyCardForCustomer(
      String customerId, CardCopyCardForCustomerParams params) throws Exception {
    Response response = copyCardForCustomerRaw(customerId, params);
    return parseResponse(response, CardCopyCardForCustomerResponse.class);
  }

  /** retrieve a card (executes immediately) - returns raw Response. */
  Response retrieveRaw(String customerId) throws Exception {
    String path = buildPathWithParams("/cards/{customer-id}", "customer-id", customerId);

    return get(path, null);
  }

  public CardRetrieveResponse retrieve(String customerId) throws Exception {
    Response response = retrieveRaw(customerId);
    return parseResponse(response, CardRetrieveResponse.class);
  }

  /** switchGatewayForCustomer a card (executes immediately) - returns raw Response. */
  Response switchGatewayForCustomerRaw(String customerId) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/switch_gateway", "customer-id", customerId);

    return post(path, null);
  }

  /**
   * switchGatewayForCustomer a card using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response switchGatewayForCustomerRaw(String customerId, CardSwitchGatewayForCustomerParams params)
      throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/switch_gateway", "customer-id", customerId);
    return post(path, params.toFormData());
  }

  /**
   * switchGatewayForCustomer a card using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response switchGatewayForCustomerRaw(String customerId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/switch_gateway", "customer-id", customerId);
    return postJson(path, jsonPayload);
  }

  public CardSwitchGatewayForCustomerResponse switchGatewayForCustomer(
      String customerId, CardSwitchGatewayForCustomerParams params) throws Exception {
    Response response = switchGatewayForCustomerRaw(customerId, params);
    return parseResponse(response, CardSwitchGatewayForCustomerResponse.class);
  }

  /** deleteCardForCustomer a card (executes immediately) - returns raw Response. */
  Response deleteCardForCustomerRaw(String customerId) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/delete_card", "customer-id", customerId);

    return post(path, null);
  }

  public CardDeleteCardForCustomerResponse deleteCardForCustomer(String customerId)
      throws Exception {
    Response response = deleteCardForCustomerRaw(customerId);
    return parseResponse(response, CardDeleteCardForCustomerResponse.class);
  }

  /** updateCardForCustomer a card (executes immediately) - returns raw Response. */
  Response updateCardForCustomerRaw(String customerId) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/credit_card", "customer-id", customerId);

    return post(path, null);
  }

  /**
   * updateCardForCustomer a card using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response updateCardForCustomerRaw(String customerId, CardUpdateCardForCustomerParams params)
      throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/credit_card", "customer-id", customerId);
    return post(path, params.toFormData());
  }

  /**
   * updateCardForCustomer a card using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response updateCardForCustomerRaw(String customerId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams("/customers/{customer-id}/credit_card", "customer-id", customerId);
    return postJson(path, jsonPayload);
  }

  public CardUpdateCardForCustomerResponse updateCardForCustomer(
      String customerId, CardUpdateCardForCustomerParams params) throws Exception {
    Response response = updateCardForCustomerRaw(customerId, params);
    return parseResponse(response, CardUpdateCardForCustomerResponse.class);
  }
}
