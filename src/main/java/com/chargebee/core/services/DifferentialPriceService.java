/*
 * This file is auto-generated by Chargebee.
 * For more information on how to make changes to this file, please see the README.
 * Reach out to dx@chargebee.com for any questions.
 * Copyright 2025 Chargebee Inc.
 */

package com.chargebee.core.services;

import com.chargebee.client.ChargebeeClient;
import com.chargebee.client.request.RequestOptions;
import com.chargebee.transport.Response;

import com.chargebee.core.models.differentialPrice.params.DifferentialPriceDeleteParams;

import com.chargebee.core.models.differentialPrice.params.DifferentialPriceCreateParams;

import com.chargebee.core.models.differentialPrice.params.DifferentialPriceListParams;

import com.chargebee.core.models.differentialPrice.params.DifferentialPriceUpdateParams;

import com.chargebee.core.responses.differentialPrice.DifferentialPriceDeleteResponse;

import com.chargebee.core.responses.differentialPrice.DifferentialPriceCreateResponse;

import com.chargebee.core.responses.differentialPrice.DifferentialPriceListResponse;

import com.chargebee.core.responses.differentialPrice.DifferentialPriceRetrieveResponse;

import com.chargebee.core.responses.differentialPrice.DifferentialPriceUpdateResponse;

public final class DifferentialPriceService extends BaseService<DifferentialPriceService> {

  private final ServiceConfig config;

  public DifferentialPriceService(ChargebeeClient client) {
    super(client);
    this.config = ServiceConfig.defaultConfig();
  }

  private DifferentialPriceService(ChargebeeClient client, RequestOptions options) {
    super(client, options);
    this.config = ServiceConfig.defaultConfig();
  }

  private DifferentialPriceService(
      ChargebeeClient client, RequestOptions options, ServiceConfig config) {
    super(client, options);
    this.config = config;
  }

  @Override
  DifferentialPriceService with(RequestOptions newOptions) {
    return new DifferentialPriceService(client, newOptions, config);
  }

  /**
   * Apply per-request options for this service instance. Users can chain .withOptions or .options
   * to set headers and other options.
   */
  public DifferentialPriceService withOptions(RequestOptions options) {
    return with(options);
  }

  // === Operations ===

  /** delete a differentialPrice (executes immediately) - returns raw Response. */
  Response deleteRaw(String differentialPriceId) throws Exception {
    String path =
        buildPathWithParams(
            "/differential_prices/{differential-price-id}/delete",
            "differential-price-id",
            differentialPriceId);

    return post(path, null);
  }

  /**
   * delete a differentialPrice using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response deleteRaw(String differentialPriceId, DifferentialPriceDeleteParams params)
      throws Exception {
    String path =
        buildPathWithParams(
            "/differential_prices/{differential-price-id}/delete",
            "differential-price-id",
            differentialPriceId);
    return post(path, params.toFormData());
  }

  /**
   * delete a differentialPrice using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response deleteRaw(String differentialPriceId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/differential_prices/{differential-price-id}/delete",
            "differential-price-id",
            differentialPriceId);
    return postJson(path, jsonPayload);
  }

  public DifferentialPriceDeleteResponse delete(
      String differentialPriceId, DifferentialPriceDeleteParams params) throws Exception {
    Response response = deleteRaw(differentialPriceId, params);
    return parseResponse(response, DifferentialPriceDeleteResponse.class);
  }

  /** create a differentialPrice (executes immediately) - returns raw Response. */
  Response createRaw(String itemPriceId) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/differential_prices", "item-price-id", itemPriceId);

    return post(path, null);
  }

  /**
   * create a differentialPrice using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response createRaw(String itemPriceId, DifferentialPriceCreateParams params) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/differential_prices", "item-price-id", itemPriceId);
    return post(path, params.toFormData());
  }

  /**
   * create a differentialPrice using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response createRaw(String itemPriceId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/item_prices/{item-price-id}/differential_prices", "item-price-id", itemPriceId);
    return postJson(path, jsonPayload);
  }

  public DifferentialPriceCreateResponse create(
      String itemPriceId, DifferentialPriceCreateParams params) throws Exception {
    Response response = createRaw(itemPriceId, params);
    return parseResponse(response, DifferentialPriceCreateResponse.class);
  }

  /**
   * list a differentialPrice using immutable params (executes immediately) - returns raw Response.
   */
  Response listRaw(DifferentialPriceListParams params) throws Exception {

    return get("/differential_prices", params != null ? params.toQueryParams() : null);
  }

  /** list a differentialPrice without params (executes immediately) - returns raw Response. */
  Response listRaw() throws Exception {

    return get("/differential_prices", null);
  }

  /**
   * list a differentialPrice using raw JSON payload (executes immediately) - returns raw Response.
   */
  Response listRaw(String jsonPayload) throws Exception {

    throw new UnsupportedOperationException("JSON payload not supported for GET operations");
  }

  public DifferentialPriceListResponse list(DifferentialPriceListParams params) throws Exception {
    Response response = listRaw(params);

    return DifferentialPriceListResponse.fromJson(response.getBodyAsString(), this, params);
  }

  public DifferentialPriceListResponse list() throws Exception {
    Response response = listRaw();
    return DifferentialPriceListResponse.fromJson(response.getBodyAsString(), this, null);
  }

  /** retrieve a differentialPrice (executes immediately) - returns raw Response. */
  Response retrieveRaw(String differentialPriceId) throws Exception {
    String path =
        buildPathWithParams(
            "/differential_prices/{differential-price-id}",
            "differential-price-id",
            differentialPriceId);

    return get(path, null);
  }

  public DifferentialPriceRetrieveResponse retrieve(String differentialPriceId) throws Exception {
    Response response = retrieveRaw(differentialPriceId);
    return parseResponse(response, DifferentialPriceRetrieveResponse.class);
  }

  /** update a differentialPrice (executes immediately) - returns raw Response. */
  Response updateRaw(String differentialPriceId) throws Exception {
    String path =
        buildPathWithParams(
            "/differential_prices/{differential-price-id}",
            "differential-price-id",
            differentialPriceId);

    return post(path, null);
  }

  /**
   * update a differentialPrice using immutable params (executes immediately) - returns raw
   * Response.
   */
  Response updateRaw(String differentialPriceId, DifferentialPriceUpdateParams params)
      throws Exception {
    String path =
        buildPathWithParams(
            "/differential_prices/{differential-price-id}",
            "differential-price-id",
            differentialPriceId);
    return post(path, params.toFormData());
  }

  /**
   * update a differentialPrice using raw JSON payload (executes immediately) - returns raw
   * Response.
   */
  Response updateRaw(String differentialPriceId, String jsonPayload) throws Exception {
    String path =
        buildPathWithParams(
            "/differential_prices/{differential-price-id}",
            "differential-price-id",
            differentialPriceId);
    return postJson(path, jsonPayload);
  }

  public DifferentialPriceUpdateResponse update(
      String differentialPriceId, DifferentialPriceUpdateParams params) throws Exception {
    Response response = updateRaw(differentialPriceId, params);
    return parseResponse(response, DifferentialPriceUpdateResponse.class);
  }
}
